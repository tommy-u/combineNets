exe contains code for combining and evaluating nets, needs to be consolidated.
vote_example contains necessary code to run the combining nets on a per output bit basis.
committee_example contains code for averaging the output of two networks.  
